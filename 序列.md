# 序列

## array

### 初始化

```
std::array<int, 3> a = {1,2,3};
```



### 元素访问

<img src="C:\Users\sunrise\AppData\Roaming\Typora\typora-user-images\image-20250212153652633.png" alt="image-20250212153652633" style="zoom:50%;" />



#### array.at(int)

返回引用,不存在报异常

#### array[]

返回引用

#### array.front()

返回第一个元素的引用

#### array.back()

返回最后一个元素的引用

### 迭代器

#### 支持迭代器

### 容量

#### array.empty()

返回bool值

如果为空返回true

#### array.size()

返回数组元素个数

### 操作

#### array.fill(value)

以指定值填充所有

## vector

`vector` 上的常见操作复杂度（效率）如下：

- 随机访问——常数 *𝓞(1)*。
- 在末尾插入或移除元素——均摊常数 *𝓞(1)*。
- 插入或移除元素——与到 vector 结尾的距离成线性 *𝓞(n)*。

### 构造器

```c++
void print(vector<std::string> v){
    for(auto iter=v.begin();iter!=v.end();iter++){
        std::cout<<*iter<<" ";
    }
    std::cout<<endl;
}
int main() {
    // C++11 初始化器列表语法：
    std::vector<std::string> words1{"sunrise","is","a","lucky","girl","!"};
    print(words1);
    // words2 == words1
    std::vector<std::string> words2(words1.begin(), words1.end());
    print(words2);
    // words3 == words1
    std::vector<std::string> words3(words1);
    print(words3);
    // words4 是 {"sun", "sun","sun","sun","sun"}
    std::vector<std::string> words4(5, "sun");
    print(words4);
}

/*
输出结果
    sunrise is a lucky girl ! 
    sunrise is a lucky girl ! 
    sunrise is a lucky girl ! 
    sun sun sun sun sun 
*/
```

#### 访问元素

<img src="C:\Users\sunrise\AppData\Roaming\Typora\typora-user-images\image-20250212155921823.png" alt="image-20250212155921823" style="zoom:50%;" />

#### v.push_back()

如果操作后新的 [`size()`](https://zh.cppreference.com/w/cpp/container/vector/size) 大于原 [`capacity()`](https://zh.cppreference.com/w/cpp/container/vector/capacity) 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 [`end()`](https://zh.cppreference.com/w/cpp/container/vector/end) 迭代器）和所有引用均会失效。否则仅有 [`end()`](https://zh.cppreference.com/w/cpp/container/vector/end) 迭代器失效。

#### v.pop_back()

指向最后元素的迭代器和引用失效。[`end()`](https://zh.cppreference.com/w/cpp/container/vector/end) 迭代器也失效。

## deque



`std::deque`（double-ended queue，双端队列）是有索引的序列容器，允许在它的首尾两端快速插入及删除。另外，在 deque 任一端的插入或删除不会使指向其余元素的指针或引用失效。

deque 的元素不是连续存储的.

### assign(count,value)

void assign( size_type count, const T& value );

## list

### assign(count,value)

void assign( size_type count, const T& value );

`list` 是支持从容器任何位置进行常数时间的元素插入和移除的容器。不支持快速随机访问。它通常实现为双向链表